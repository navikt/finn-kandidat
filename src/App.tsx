import React, { FunctionComponent } from 'react';
import './app.less';

const App: FunctionComponent = () => (
    <div className="app">
        <header className="app--header">
            <h1>Finn kandidat</h1>
        </header>
    </div>
);

export default App;
